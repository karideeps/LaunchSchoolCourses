extends layout.pug 

block appContent
    h1 Deepak Karivelil
    h2 You have: #{money}
    if !recentTransaction
        p No recent transactions.
    else
        p Recent transaction.
    button Logout