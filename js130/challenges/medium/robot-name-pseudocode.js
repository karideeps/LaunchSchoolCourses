/*
declare class `Robot`

  static chosenNames = [];

  constructor()
    this.name = this.name();

  name() 
    if (this.name) return this.name
    return generateRandomName();

  generateRandomName() 
    let name = // code to generateRandomName
    while (choseName.includes(Name))
      name = generateRandomName
    return name;


  reset()
    this.name = generateRandomName()

*/